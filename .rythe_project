local project = {
    alias = "rsl",
    namespace = "rsl",
    additional_types = { "test" },
    dependencies = {
        "public third_party/spdlog"
    },
    defines = { "RYTHE_INTERNAL" }
}

return project
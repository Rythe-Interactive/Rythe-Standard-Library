local project = {
    alias = "rsl",
    namespace = "rsl",
    warnings_as_errors = false,
    dependencies = {
        "public third_party/spdlog:header-only"
    },
    defines = { "RYTHE_INTERNAL" }
}

return project